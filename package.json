{
  "name": "palette-genius",
  "version": "1.0.0",
  "description": "a package that generates random color palettes based on a specified brand",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gtchakama",
  "license": "ISC",
  "dependencies": {
    "chroma-js": "^2.4.2"
  }
}
